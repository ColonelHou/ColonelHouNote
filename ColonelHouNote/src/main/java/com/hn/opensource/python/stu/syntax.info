guess = int(raw_input('Enter a number')) # 内置函数raw_input()转换成int(还有len(''))输入值赋值给guess
----------------if else===================================
if x == y :
elif x > y :
else:  #这里的else可有可无
-------------
在python中 None,  False, 空字符串"", 0, 空列表[], 空字典{}, 空元组()都相当于False
if x is not None`
第一种是`if x is None`；
第二种是 `if not x：`；
第三种是`if not x is None`（这句这样理解更清晰`if not (x is None)`） 。
-----------------while ========================================
while running:
    syn  # if xxx :break
else:
    syn
-----------------for =======================================
for i in range(1, 5):
    syn
else:
    syn
-----------------function============================
def sayHello(a, b=90):  #给b默认值; sayHello(c=90, d=100)